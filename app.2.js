var carmel= 20;
if (carmel >= 16) {return true} else {return false}

function discount(total, discount){
   return  (total - discount=num) + num.value= "$" + num.toFixed(2)
}

total = 20;
discount = 2.50;

function discount (20,2.50);
